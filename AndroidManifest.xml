<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.saukakke.dbnaco" >
	
	<uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
		<activity 
			android:name=".Dashboard"
			android:label="DB NACO Dashboard"/>
		<activity 
			android:name=".Add"
			android:label="Add Comrade Record"/>
		<activity 
			android:name=".Search"
			android:label="Search Comrade Record"/>
		<activity 
			android:name=".Update"
			android:label="Update Comrade Record"/>
		<activity 
			android:name=".ViewRecords"
			android:label="View All Records"/>
    </application>

</manifest>
